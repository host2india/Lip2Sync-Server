# Wav2Lip Single Image ENGINE (single_image.mp4 output)
- Converts a single image + audio into a talking video (60fps)
- API: POST /api/sync/single_image
- Inputs: image file (face), audio file (wav/mp3)
- Output filename returned: single_image.mp4
